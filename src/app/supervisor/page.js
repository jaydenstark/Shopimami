import SupervisorApp from '../../components/SupervisorApp';

export const metadata = {
  title: 'SHOPIMAMI | Supervisor Console',
  description: 'Operations control center for active and flagged order lifecycle audits.',
};

export default function Page() {
  return <SupervisorApp />;
}
