import RiderApp from '../../components/RiderApp';

export const metadata = {
  title: 'SHOPIMAMI | Dispatch Rider App',
  description: 'Dispatch Rider interface for pickup and delivery routing.',
};

export default function Page() {
  return <RiderApp />;
}
