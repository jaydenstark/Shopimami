import CustomerApp from '../../components/CustomerApp';

export const metadata = {
  title: 'MallMart | Customer Shopping Portal',
  description: 'Shop from physical Accra, West Hills, and A&C Mall stores with direct MoMo payment and end-to-end tracking.',
};

export default function Page() {
  return <CustomerApp />;
}
